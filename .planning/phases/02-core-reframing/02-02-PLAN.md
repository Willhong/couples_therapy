---
phase: 02-core-reframing
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - backend/apps/chat/services/__init__.py
  - backend/apps/chat/services/claude_service.py
  - backend/apps/chat/services/context_manager.py
  - backend/apps/chat/services/reframing_service.py
  - backend/apps/chat/prompts/__init__.py
  - backend/apps/chat/prompts/system_prompts.py
  - backend/apps/chat/views.py
  - backend/requirements.txt
autonomous: true
user_setup:
  - service: anthropic
    why: "Claude API for AI reframing responses"
    env_vars:
      - name: ANTHROPIC_API_KEY
        source: "Anthropic Console -> API Keys -> Create key"
    dashboard_config: []

must_haves:
  truths:
    - "AI responds to user messages with streaming text"
    - "Context window is managed via rolling summarization"
    - "AI response follows structured reframing format"
    - "AI never assigns blame or declares who is right/wrong"
  artifacts:
    - path: "backend/apps/chat/services/claude_service.py"
      provides: "Claude API streaming integration"
      contains: "async with client.messages.stream"
    - path: "backend/apps/chat/services/context_manager.py"
      provides: "Rolling conversation summarization"
      contains: "class ConversationContextManager"
    - path: "backend/apps/chat/prompts/system_prompts.py"
      provides: "Non-judgmental reframing prompt"
      contains: "REFRAMING_SYSTEM_PROMPT"
  key_links:
    - from: "backend/apps/chat/views.py"
      to: "backend/apps/chat/services/claude_service.py"
      via: "streaming endpoint imports service"
      pattern: "from apps\\.chat\\.services\\.claude_service import"
    - from: "backend/apps/chat/services/claude_service.py"
      to: "anthropic"
      via: "AsyncAnthropic client"
      pattern: "AsyncAnthropic\\(\\)"
---

<objective>
Integrate Claude API with Django for streaming AI reframing responses.

Purpose: Enable real-time AI responses that provide perspective reframing. The streaming approach ensures good UX (user sees response building). Context management ensures conversations can be long without exceeding token limits.

Output: Claude service with streaming, context manager with rolling summarization, and streaming HTTP endpoint.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-core-reframing/02-CONTEXT.md
@.planning/phases/02-core-reframing/02-RESEARCH.md
@backend/apps/chat/models.py
@backend/apps/chat/views.py
@backend/config/settings/base.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create system prompts for non-judgmental reframing</name>
  <files>
    backend/apps/chat/prompts/__init__.py
    backend/apps/chat/prompts/system_prompts.py
  </files>
  <action>
Create prompts directory and system prompts per RESEARCH.md:

**system_prompts.py:**

```python
REFRAMING_SYSTEM_PROMPT = """당신은 커플 관계 코치입니다. 사용자가 파트너와의 갈등 상황을 설명하면, 관점 전환(리프레이밍)을 통해 상호 이해를 돕습니다.

## 핵심 원칙

1. **절대 판단하지 않음**: "누가 맞다/틀리다", "당신이 잘못했다", "상대방이 나쁘다" 등의 표현 금지
2. **양방향 관점 제시**:
   - "당신이 말한 것이 상대방에게 어떻게 들렸을 수 있는지"
   - "상대방이 말한 것을 당신이 어떻게 해석했을 수 있는지"
3. **구체적 행동 제안**: 추상적 조언 대신 바로 실행할 수 있는 구체적 행동 제시
4. **감정 인정 우선**: 분석 전에 사용자의 감정을 먼저 인정하고 공감

## 응답 구조

다음 JSON 구조로 응답하세요:

{
  "acknowledgment": "사용자 감정에 대한 공감 표현",
  "what_you_said": "사용자 메시지에서 핵심 표현 인용",
  "how_they_heard": "동일한 말이 상대방 관점에서 어떻게 해석될 수 있는지",
  "how_you_heard_them": "상대방의 말을 사용자가 어떻게 해석했을 수 있는지 (상대방 언급 시)",
  "why_the_gap": "소통 방식, 과거 경험, 감정 상태 등 차이의 원인 분석",
  "suggestions": ["구체적 행동 제안 1", "구체적 행동 제안 2", "구체적 행동 제안 3"],
  "original_quotes": ["사용자 원문에서 인용한 핵심 문구들"]
}

## 금지 사항

- "상대방이 잘못했네요", "당신 말이 맞아요" 등 판단
- "항상", "절대", "모든" 등 절대적 표현
- 관계를 끝내라는 조언 (학대 상황 제외)
- 전문 상담 없이 심리 진단

## 학대 패턴 감지 시

폭력, 통제, 고립 등 학대 신호 감지 시:
1. 먼저 사용자 안전 확인
2. 전문 상담 자원 안내 (1366 여성긴급전화 등)
3. 판단 없이 사용자 선택 존중
응답에 "abuse_flag": true 추가

## 언어

한국어로 응답하세요. 격식체("~입니다", "~세요")를 사용하되 따뜻하고 지지적인 톤을 유지하세요."""


SUMMARIZATION_PROMPT = """다음 대화 내용을 핵심 정보만 보존하여 요약해주세요:
- 갈등의 주요 주제와 맥락
- 언급된 감정과 우려사항
- 파트너에 대한 중요한 정보
- 아직 해결되지 않은 문제

대화 내용:
{conversation}

200자 이내로 요약:"""


AI_THINKING_MESSAGES = [
    "상대방 관점을 분석하고 있어요...",
    "양측의 감정을 이해하고 있어요...",
    "소통 패턴을 살펴보고 있어요...",
    "구체적인 제안을 준비하고 있어요...",
]
```

The system prompt:
- Enforces non-judgmental stance (REFR-04)
- Requires bidirectional reframing (REFR-01)
- Demands concrete suggestions (REFR-02)
- Outputs structured JSON for parsing into reframing modal sections
- Includes abuse detection with safety resources
  </action>
  <verify>
```bash
cd backend
python -c "from apps.chat.prompts.system_prompts import REFRAMING_SYSTEM_PROMPT; print('Prompt loaded:', len(REFRAMING_SYSTEM_PROMPT), 'chars')"
```
Import succeeds and prints prompt length.
  </verify>
  <done>
System prompts defined with structured JSON output format for reframing.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Claude service and context manager</name>
  <files>
    backend/apps/chat/services/__init__.py
    backend/apps/chat/services/claude_service.py
    backend/apps/chat/services/context_manager.py
    backend/requirements.txt
  </files>
  <action>
Add anthropic to requirements.txt:
```
anthropic>=0.52.0
```

Create services directory with:

**claude_service.py:**
```python
import json
from typing import AsyncGenerator
from django.conf import settings
from anthropic import AsyncAnthropic

from apps.chat.prompts.system_prompts import REFRAMING_SYSTEM_PROMPT

client = AsyncAnthropic()  # Uses ANTHROPIC_API_KEY env var


async def generate_reframing_stream(
    conversation_context: str,
    user_message: str
) -> AsyncGenerator[str, None]:
    """
    Async generator that yields Claude streaming response chunks.
    Yields SSE-formatted data for client consumption.
    """
    try:
        async with client.messages.stream(
            model="claude-sonnet-4-5-20250929",
            max_tokens=2048,
            system=REFRAMING_SYSTEM_PROMPT,
            messages=[
                {
                    "role": "user",
                    "content": f"{conversation_context}\n\n최근 메시지: {user_message}"
                }
            ],
        ) as stream:
            async for text in stream.text_stream:
                # SSE format: data: <content>\n\n
                yield f"data: {text}\n\n"

        # Signal completion
        yield "data: [DONE]\n\n"

    except Exception as e:
        yield f"data: [ERROR] {str(e)}\n\n"


def parse_reframing_response(response_text: str) -> dict:
    """
    Parse Claude's JSON response into structured reframing data.
    Returns dict with sections for ReframingModal.
    """
    try:
        # Find JSON in response (may have markdown code blocks)
        json_start = response_text.find('{')
        json_end = response_text.rfind('}') + 1
        if json_start >= 0 and json_end > json_start:
            json_str = response_text[json_start:json_end]
            return json.loads(json_str)
    except json.JSONDecodeError:
        pass

    # Fallback: return raw text if JSON parsing fails
    return {
        "acknowledgment": "",
        "what_you_said": "",
        "how_they_heard": response_text,
        "how_you_heard_them": "",
        "why_the_gap": "",
        "suggestions": [],
        "original_quotes": []
    }
```

**context_manager.py:**
```python
from typing import Optional
from anthropic import Anthropic
from apps.chat.prompts.system_prompts import SUMMARIZATION_PROMPT

client = Anthropic()  # Sync client for summarization


class ConversationContextManager:
    """
    Manages conversation context using rolling summarization.
    Keeps recent messages verbatim + summarized older context.
    """

    MAX_RECENT_MESSAGES = 10  # Keep last 10 messages verbatim
    SUMMARY_TRIGGER_COUNT = 20  # Summarize when exceeding this

    def __init__(self, conversation_id):
        self.conversation_id = conversation_id
        self._cached_summary = None

    def get_context_for_ai(self) -> str:
        """
        Returns optimized context string for Claude API.
        Format: [Summary of older messages] + [Recent messages verbatim]
        """
        from apps.chat.models import Message, ConversationSummary

        messages = Message.objects.filter(
            conversation_id=self.conversation_id
        ).order_by('-created_at')

        total_count = messages.count()

        if total_count <= self.MAX_RECENT_MESSAGES:
            # Few messages: return all verbatim
            return self._format_messages(list(messages.reverse()))

        # Get recent messages verbatim
        recent_messages = list(messages[:self.MAX_RECENT_MESSAGES])
        recent_messages.reverse()

        # Get or create summary of older messages
        summary = self._get_or_create_summary()

        context_parts = []
        if summary:
            context_parts.append(f"[이전 대화 요약]\n{summary}\n")
        context_parts.append("[최근 대화]")
        context_parts.append(self._format_messages(recent_messages))

        return "\n".join(context_parts)

    def _get_or_create_summary(self) -> str:
        """Get cached summary or generate new one."""
        from apps.chat.models import Message, ConversationSummary

        # Check for existing valid summary
        existing = ConversationSummary.objects.filter(
            conversation_id=self.conversation_id
        ).order_by('-created_at').first()

        if existing and self._is_summary_current(existing):
            return existing.summary_text

        # Generate new summary
        older_messages = Message.objects.filter(
            conversation_id=self.conversation_id
        ).order_by('created_at')[:-self.MAX_RECENT_MESSAGES]

        if not older_messages.exists():
            return ""

        summary_text = self._generate_summary(list(older_messages))

        ConversationSummary.objects.create(
            conversation_id=self.conversation_id,
            summary_text=summary_text,
            message_count=older_messages.count()
        )

        return summary_text

    def _generate_summary(self, messages) -> str:
        """Use Claude Haiku to summarize older messages."""
        formatted = self._format_messages(messages)

        response = client.messages.create(
            model="claude-haiku-3-5-20241022",
            max_tokens=300,
            messages=[{
                "role": "user",
                "content": SUMMARIZATION_PROMPT.format(conversation=formatted)
            }]
        )

        return response.content[0].text

    def _format_messages(self, messages) -> str:
        """Format messages for Claude context."""
        lines = []
        for msg in messages:
            role = "나" if msg.role == 'user' else "AI"
            lines.append(f"{role}: {msg.content}")
        return "\n".join(lines)

    def _is_summary_current(self, summary) -> bool:
        """Check if summary is still valid."""
        from apps.chat.models import Message
        current_count = Message.objects.filter(
            conversation_id=self.conversation_id
        ).count()
        # Summary is current if message count hasn't grown too much
        return current_count - summary.message_count < self.SUMMARY_TRIGGER_COUNT
```

Run `pip install -r requirements.txt` to install anthropic.
  </action>
  <verify>
```bash
cd backend
pip install -r requirements.txt
python -c "from apps.chat.services.claude_service import generate_reframing_stream; print('Claude service loaded')"
python -c "from apps.chat.services.context_manager import ConversationContextManager; print('Context manager loaded')"
```
Both imports succeed.
  </verify>
  <done>
Claude service with streaming and context manager with rolling summarization are functional.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create streaming HTTP endpoint</name>
  <files>
    backend/apps/chat/views.py
    backend/apps/chat/urls.py
  </files>
  <action>
Add streaming reframing endpoint to chat views:

**views.py** (add to existing):
```python
from django.http import StreamingHttpResponse
from rest_framework.decorators import api_view, permission_classes
from rest_framework.permissions import IsAuthenticated
from asgiref.sync import sync_to_async

from apps.chat.services.claude_service import generate_reframing_stream, parse_reframing_response
from apps.chat.services.context_manager import ConversationContextManager


@api_view(['POST'])
@permission_classes([IsAuthenticated])
def stream_reframing(request):
    """
    Streaming endpoint for Claude reframing response.
    Uses async StreamingHttpResponse for SSE.

    Request body:
    {
        "conversation_id": "uuid",
        "message": "user's message text"
    }

    Response: SSE stream with reframing chunks
    """
    conversation_id = request.data.get('conversation_id')
    message = request.data.get('message')

    if not conversation_id or not message:
        return Response(
            {"error": "conversation_id와 message가 필요합니다"},
            status=400
        )

    # Verify user owns conversation
    try:
        conversation = Conversation.objects.get(
            id=conversation_id,
            user=request.user
        )
    except Conversation.DoesNotExist:
        return Response(
            {"error": "대화를 찾을 수 없습니다"},
            status=404
        )

    # Save user message
    user_msg = Message.objects.create(
        conversation=conversation,
        role='user',
        content=message
    )

    # Get conversation context
    context_manager = ConversationContextManager(conversation_id)
    context = context_manager.get_context_for_ai()

    # Create streaming response
    response = StreamingHttpResponse(
        generate_reframing_stream(context, message),
        content_type='text/event-stream'
    )
    response['Cache-Control'] = 'no-cache'
    response['X-Accel-Buffering'] = 'no'  # Disable nginx buffering
    return response


@api_view(['POST'])
@permission_classes([IsAuthenticated])
def save_reframing(request):
    """
    Save completed reframing response as assistant message.
    Called by client after streaming completes.

    Request body:
    {
        "conversation_id": "uuid",
        "content": "full AI response text",
        "reframing_data": { parsed JSON structure }
    }
    """
    conversation_id = request.data.get('conversation_id')
    content = request.data.get('content')
    reframing_data = request.data.get('reframing_data')

    if not conversation_id or not content:
        return Response(
            {"error": "conversation_id와 content가 필요합니다"},
            status=400
        )

    # Verify ownership
    try:
        conversation = Conversation.objects.get(
            id=conversation_id,
            user=request.user
        )
    except Conversation.DoesNotExist:
        return Response(
            {"error": "대화를 찾을 수 없습니다"},
            status=404
        )

    # Parse reframing if not provided
    if not reframing_data:
        reframing_data = parse_reframing_response(content)

    # Save assistant message
    assistant_msg = Message.objects.create(
        conversation=conversation,
        role='assistant',
        content=content,
        has_reframing=True,
        reframing_data=reframing_data
    )

    # Update conversation title if first exchange
    if not conversation.title and Message.objects.filter(conversation=conversation).count() <= 2:
        # Use first 50 chars of user message as title
        first_user_msg = Message.objects.filter(
            conversation=conversation,
            role='user'
        ).first()
        if first_user_msg:
            conversation.title = first_user_msg.content[:50]
            if len(first_user_msg.content) > 50:
                conversation.title += "..."
            conversation.save()

    return Response(MessageSerializer(assistant_msg).data, status=201)
```

**urls.py** (add routes):
```python
urlpatterns = [
    # ... existing routes ...
    path('stream-reframing/', stream_reframing, name='stream-reframing'),
    path('save-reframing/', save_reframing, name='save-reframing'),
]
```

Import Message, Conversation models and MessageSerializer at top of views.py.
  </action>
  <verify>
```bash
cd backend
python manage.py check
# Test endpoint exists (will fail without API key, but route should work)
curl -X POST http://localhost:8000/api/v1/chat/stream-reframing/ \
  -H "Content-Type: application/json" \
  -d '{"conversation_id": "test", "message": "test"}'
```
Django check passes. Endpoint returns 401 (unauthorized) or 400 (bad request) as expected.
  </verify>
  <done>
Streaming reframing endpoint responds to POST requests. Save-reframing endpoint stores completed AI responses.
  </done>
</task>

</tasks>

<verification>
1. `pip install -r requirements.txt` installs anthropic SDK
2. `python manage.py check` passes
3. System prompt imports correctly
4. Claude service async generator can be imported
5. Context manager can format messages and detect summary needs
6. Streaming endpoint returns SSE content-type
7. Save-reframing endpoint persists assistant messages with reframing_data
</verification>

<success_criteria>
- anthropic SDK installed and importable
- System prompts enforce non-judgmental, bidirectional reframing
- Claude service streams responses via SSE
- Context manager summarizes conversations exceeding 10 messages
- Streaming endpoint saves user message and returns AI response stream
- Save-reframing endpoint persists complete response with parsed structure
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-reframing/02-02-SUMMARY.md`
</output>
