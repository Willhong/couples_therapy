---
phase: 01-foundation-safety
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/config/settings/base.py
  - backend/config/settings/development.py
  - backend/config/urls.py
  - backend/config/asgi.py
  - backend/apps/users/models.py
  - backend/apps/users/serializers.py
  - backend/apps/users/urls.py
  - backend/apps/couples/models.py
  - backend/apps/couples/serializers.py
  - backend/apps/couples/views.py
  - backend/apps/couples/urls.py
  - backend/apps/consents/models.py
  - backend/apps/core/middleware.py
  - backend/apps/core/permissions.py
  - backend/requirements.txt
  - backend/manage.py
autonomous: true

must_haves:
  truths:
    - "Django project runs with all apps configured"
    - "Custom User model uses email as username field"
    - "JWT authentication returns access and refresh tokens"
    - "Couple and InviteCode models support partner linking"
    - "RecordingConsent model tracks dual consent"
    - "Sensitive fields are encrypted at database level"
  artifacts:
    - path: "backend/apps/users/models.py"
      provides: "Custom User model with email auth and disclaimer tracking"
      contains: "class User(AbstractUser)"
    - path: "backend/apps/couples/models.py"
      provides: "Couple and InviteCode models"
      contains: "class InviteCode"
    - path: "backend/apps/consents/models.py"
      provides: "RecordingConsent model with encrypted fields"
      contains: "EncryptedTextField"
    - path: "backend/config/settings/base.py"
      provides: "Django settings with JWT and Channels config"
      contains: "SIMPLE_JWT"
  key_links:
    - from: "backend/config/settings/base.py"
      to: "backend/apps/users/models.py"
      via: "AUTH_USER_MODEL"
      pattern: "AUTH_USER_MODEL.*=.*users\\.User"
    - from: "backend/config/urls.py"
      to: "dj_rest_auth"
      via: "include"
      pattern: "dj_rest_auth\\.urls"
---

<objective>
Set up Django backend with custom User model, JWT authentication, Couple/InviteCode models, and RecordingConsent model with field encryption.

Purpose: Backend foundation for all authentication, partner linking, and consent features. Frontend cannot function without these APIs.
Output: Running Django server with JWT auth endpoints, database models for users, couples, and consents.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-safety/01-CONTEXT.md
@.planning/phases/01-foundation-safety/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Django project with dependencies</name>
  <files>
    - backend/requirements.txt
    - backend/manage.py
    - backend/config/__init__.py
    - backend/config/settings/__init__.py
    - backend/config/settings/base.py
    - backend/config/settings/development.py
    - backend/config/wsgi.py
    - backend/config/asgi.py
    - backend/apps/__init__.py
  </files>
  <action>
Create Django project structure using `django-admin startproject config backend`.

Create `backend/requirements.txt`:
```
Django>=5.0,<6.0
djangorestframework>=3.15
djangorestframework-simplejwt>=5.3
dj-rest-auth[with_social]>=6.0
django-allauth>=0.61
channels>=4.3
channels-redis>=4.2
daphne>=4.1
djfernet>=0.9
psycopg[binary]>=3.1
redis>=5.0
django-cors-headers>=4.3
python-dotenv>=1.0
```

Install dependencies: `pip install -r backend/requirements.txt`

Restructure settings for split configuration:
- Move `config/settings.py` to `config/settings/base.py`
- Create `config/settings/__init__.py` (empty)
- Create `config/settings/development.py` that imports from base

Configure `backend/config/settings/base.py`:
- Reference 01-RESEARCH.md Pattern 1 for full configuration
- INSTALLED_APPS: Add daphne (FIRST), rest_framework, rest_framework_simplejwt, corsheaders, allauth, dj_rest_auth, channels
- Add apps.users, apps.couples, apps.consents, apps.core (create empty app folders)
- SITE_ID = 1
- REST_FRAMEWORK config with JWTAuthentication and throttling
- SIMPLE_JWT config: 30min access, 7day refresh, rotation enabled, blacklist enabled
- REST_AUTH config: USE_JWT=True, JWT_AUTH_HTTPONLY=False
- ACCOUNT settings: email as auth method, email required, username not required
- Add FERNET_KEYS setting (generate key with `from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())`)
- AUTH_USER_MODEL = 'users.User'

Configure `backend/config/settings/development.py`:
```python
from .base import *

DEBUG = True
ALLOWED_HOSTS = ['*']
CORS_ALLOW_ALL_ORIGINS = True

DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.sqlite3',
        'NAME': BASE_DIR / 'db.sqlite3',
    }
}

# Use in-memory channel layer for development
CHANNEL_LAYERS = {
    'default': {
        'BACKEND': 'channels.layers.InMemoryChannelLayer'
    }
}

# Email backend for development (console)
EMAIL_BACKEND = 'django.core.mail.backends.console.EmailBackend'
```

Create app directories:
- `backend/apps/users/__init__.py`
- `backend/apps/couples/__init__.py`
- `backend/apps/consents/__init__.py`
- `backend/apps/core/__init__.py`

Configure `backend/config/asgi.py`:
- Reference 01-RESEARCH.md Pattern 6 for ASGI setup
- ProtocolTypeRouter for http and websocket
- Import JWTAuthMiddlewareStack from apps.core.middleware (create placeholder)
  </action>
  <verify>
    - `cd backend && python manage.py check --settings=config.settings.development` passes
    - All apps recognized: `python manage.py showmigrations --settings=config.settings.development`
  </verify>
  <done>
    - Django project structure created
    - All dependencies installed
    - Settings split into base/development
    - JWT and Channels configured
  </done>
</task>

<task type="auto">
  <name>Task 2: Create User model and authentication serializers</name>
  <files>
    - backend/apps/users/models.py
    - backend/apps/users/admin.py
    - backend/apps/users/serializers.py
    - backend/apps/users/urls.py
    - backend/apps/users/apps.py
  </files>
  <action>
Create `backend/apps/users/apps.py`:
```python
from django.apps import AppConfig

class UsersConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.users'
```

Create `backend/apps/users/models.py`:
- Reference 01-RESEARCH.md Pattern 2 for full implementation
- Custom UserManager with create_user/create_superuser using email
- Custom User(AbstractUser):
  - Remove username field (username = None)
  - email as unique, required field
  - disclaimer_accepted (boolean, default False)
  - disclaimer_accepted_at (DateTimeField, null)
  - disclaimer_version (CharField, blank)
  - onboarding_completed (boolean, default False)
  - tutorial_completed (boolean, default False)
  - USERNAME_FIELD = 'email'
  - REQUIRED_FIELDS = []
  - db_table = 'users'

Create `backend/apps/users/admin.py`:
- Register User model with custom admin displaying email, disclaimer status

Create `backend/apps/users/serializers.py`:
- Reference 01-RESEARCH.md Custom Registration Serializer example
- CustomRegisterSerializer(RegisterSerializer):
  - Add disclaimer_accepted (BooleanField, required=True)
  - Add disclaimer_version (CharField, required=True)
  - validate_disclaimer_accepted: raise error if False (Korean: "면책조항에 동의해야 합니다")
  - custom_signup: set user disclaimer fields, create DisclaimerConsent record

- UserSerializer: id, email, disclaimer_accepted, onboarding_completed, tutorial_completed

Create `backend/apps/users/urls.py`:
- Include simplejwt token views: obtain, refresh, verify
- Reference 01-RESEARCH.md Pattern 7
  </action>
  <verify>
    - `python manage.py makemigrations users --settings=config.settings.development` creates migration
    - `python manage.py migrate --settings=config.settings.development` succeeds
    - Model imports without errors
  </verify>
  <done>
    - Custom User model with email authentication
    - Disclaimer tracking fields
    - Registration serializer with disclaimer validation
    - JWT token endpoints configured
  </done>
</task>

<task type="auto">
  <name>Task 3: Create Couple, InviteCode, and Consent models with API</name>
  <files>
    - backend/apps/couples/models.py
    - backend/apps/couples/serializers.py
    - backend/apps/couples/views.py
    - backend/apps/couples/urls.py
    - backend/apps/couples/apps.py
    - backend/apps/consents/models.py
    - backend/apps/consents/serializers.py
    - backend/apps/consents/apps.py
    - backend/apps/core/middleware.py
    - backend/apps/core/permissions.py
    - backend/apps/core/apps.py
    - backend/config/urls.py
  </files>
  <action>
Create app configs (apps.py) for couples, consents, core.

Create `backend/apps/couples/models.py`:
- Reference 01-RESEARCH.md Pattern 3 for full implementation
- generate_invite_code() function: 6 chars from 'ABCDEFGHJKLMNPQRSTUVWXYZ23456789'
- Couple model:
  - Status choices: PENDING, ACTIVE, DISCONNECTED
  - user1, user2 (ForeignKey to User)
  - status, connected_at, disconnected_at, timestamps
  - get_partner(user) method
  - db_table = 'couples'
  - CheckConstraint: cannot_couple_with_self

- InviteCode model:
  - code (6 chars, unique, default=generate_invite_code)
  - creator (ForeignKey to User)
  - couple (ForeignKey to Couple)
  - expires_at (default: now + 24 hours)
  - used_by, used_at
  - is_valid property
  - create_for_user classmethod
  - db_table = 'invite_codes'

Create `backend/apps/consents/models.py`:
- Reference 01-RESEARCH.md Pattern 4 for full implementation
- RecordingConsent model:
  - Status choices: PENDING, BOTH_CONSENTED, DECLINED, EXPIRED
  - couple (ForeignKey)
  - session_id (UUIDField, unique)
  - requester, responder (ForeignKeys to User)
  - requester_consented, requester_consented_at
  - responder_consented, responder_consented_at
  - requester_ip, responder_ip (EncryptedTextField from djfernet)
  - status, created_at, expires_at
  - process_response(user, consented, ip) method
  - db_table = 'recording_consents'

- DisclaimerConsent model:
  - user (ForeignKey)
  - version, content_hash
  - consented_at
  - ip_address (EncryptedTextField)
  - user_agent
  - db_table = 'disclaimer_consents'

Create `backend/apps/couples/serializers.py`:
- InviteCodeSerializer: code, expires_at, deep_link (SerializerMethodField)
- CoupleSerializer: id, status, connected_at, partner (nested user info)

Create `backend/apps/couples/views.py`:
- Reference 01-RESEARCH.md Partner Invite API Views example
- InviteCodeViewSet:
  - generate action: create new code, return code + deep_link
  - redeem action: validate code, connect couple, return couple info
  - Korean error messages from CONTEXT.md

- CoupleViewSet:
  - list: get current user's active couple
  - disconnect action: set status to disconnected

Create `backend/apps/couples/urls.py`:
- Router with invite and couple viewsets

Create `backend/apps/core/middleware.py`:
- Reference 01-RESEARCH.md Pattern 6 for JWTAuthMiddleware
- JWTAuthMiddlewareStack wrapper function

Create `backend/apps/core/permissions.py`:
- IsCouplePartner permission: check if user is part of couple
- IsConsentParticipant permission: check if user is requester or responder

Create `backend/config/urls.py`:
- Reference 01-RESEARCH.md Pattern 7
- Include dj_rest_auth.urls at api/v1/auth/
- Include dj_rest_auth.registration.urls at api/v1/auth/registration/
- Include users.urls at api/v1/auth/token/
- Include couples.urls at api/v1/couples/

Run migrations:
```bash
python manage.py makemigrations couples consents --settings=config.settings.development
python manage.py migrate --settings=config.settings.development
```
  </action>
  <verify>
    - All migrations apply: `python manage.py migrate --settings=config.settings.development`
    - Server starts: `python manage.py runserver --settings=config.settings.development`
    - API endpoints accessible: `curl http://localhost:8000/api/v1/auth/registration/`
  </verify>
  <done>
    - Couple and InviteCode models ready
    - RecordingConsent with encrypted fields
    - Partner invitation API endpoints working
    - JWT middleware for WebSocket ready
  </done>
</task>

</tasks>

<verification>
After all tasks complete:
1. `python manage.py check --settings=config.settings.development` passes
2. `python manage.py runserver --settings=config.settings.development` starts without errors
3. Create user via API: `curl -X POST http://localhost:8000/api/v1/auth/registration/ -H "Content-Type: application/json" -d '{"email":"test@test.com","password1":"testpass123","password2":"testpass123","disclaimer_accepted":true,"disclaimer_version":"1.0"}'`
4. Obtain JWT token: `curl -X POST http://localhost:8000/api/v1/auth/token/ -H "Content-Type: application/json" -d '{"email":"test@test.com","password":"testpass123"}'`
5. Token response includes access and refresh tokens
</verification>

<success_criteria>
- Django project runs with all apps loaded
- Custom User model uses email for authentication
- JWT tokens issued on login with proper lifetimes
- Couple, InviteCode, RecordingConsent models migrated
- Encrypted fields use djfernet
- API structure matches 01-RESEARCH.md patterns
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-safety/01-01-SUMMARY.md`
</output>
