---
phase: 03-audio-pipeline
plan: 05
type: execute
wave: 3
depends_on: ["03-02", "03-03"]
files_modified:
  - mobile/src/features/recording/components/LiveConsentFlow.tsx
  - mobile/src/features/recording/components/RecordingScreen.tsx
  - mobile/src/features/recording/hooks/useLiveRecording.ts
  - backend/apps/consents/consumers.py
  - backend/apps/audio/views.py
autonomous: true

must_haves:
  truths:
    - "Initiator taps live record -> partner receives consent request via WebSocket"
    - "If partner declines -> user offered self-narration as alternative"
    - "Either partner can stop recording at any time"
    - "Live recording requires per-session consent from both partners"
    - "Recorded audio is uploaded and diarized with speaker separation"
  artifacts:
    - path: "mobile/src/features/recording/components/LiveConsentFlow.tsx"
      provides: "WebSocket consent request and waiting UI"
      min_lines: 60
    - path: "mobile/src/features/recording/hooks/useLiveRecording.ts"
      provides: "Live recording state management with consent"
      min_lines: 40
  key_links:
    - from: "mobile/src/features/recording/components/LiveConsentFlow.tsx"
      to: "WebSocket consent channel"
      via: "ws:// connection for consent request/response"
      pattern: "WebSocket|ws://"
    - from: "mobile/src/features/recording/components/RecordingScreen.tsx"
      to: "mobile/src/features/recording/components/LiveConsentFlow.tsx"
      via: "rendered when mode='live'"
      pattern: "LiveConsentFlow"
---

<objective>
Add live conflict recording support: partner consent flow via WebSocket, live recording with diarization, and either-partner-can-stop functionality. Extends the recording feature from Plan 02 with the second recording mode.

Purpose: Live conflict recording captures actual arguments between partners with speaker separation, providing the deepest level of conflict insight. Consent flow is critical for privacy.
Output: Working live recording mode with real-time consent, recording, and diarized transcription.
</objective>

<execution_context>
@C:\Users\Hong\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Hong\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-audio-pipeline/03-CONTEXT.md
@.planning/phases/03-audio-pipeline/03-02-SUMMARY.md
@.planning/phases/03-audio-pipeline/03-03-SUMMARY.md

Key existing files to reference:
@backend/apps/consents/models.py (RecordingConsent model -- already exists from Phase 1)
@backend/apps/consents/consumers.py (WebSocket consumer for consent -- already exists from Phase 1)
@backend/apps/consents/views.py (consent API)
@mobile/src/features/recording/components/RecordingScreen.tsx (extend with live mode)
@mobile/src/features/recording/hooks/useAudioRecording.ts (reuse for recording)
@mobile/src/components/consent/DualConsentPrompt.tsx (existing consent UI pattern)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Live consent flow component + live recording hook</name>
  <files>
    mobile/src/features/recording/components/LiveConsentFlow.tsx
    mobile/src/features/recording/hooks/useLiveRecording.ts
  </files>
  <action>
    1. Create mobile/src/features/recording/hooks/useLiveRecording.ts:
       - Manages the full live recording lifecycle: consent -> record -> upload
       - State: phase ('requesting_consent' | 'waiting_consent' | 'consent_granted' | 'consent_declined' | 'recording' | 'stopped')
       - Uses WebSocket to send consent request to partner (reuse existing consent WebSocket pattern from Phase 1)
       - requestConsent(): Opens WebSocket to consent channel, sends consent_request message with session_id
       - Listens for consent_response: if accepted, sets phase to 'consent_granted'; if declined, sets to 'consent_declined'
       - Consent timeout: 5 minutes (matches backend RecordingConsent model)
       - startRecording(): only callable when phase='consent_granted'. Uses useAudioRecording hook internally
       - stopRecording(): stops recording, gets URI. Either user can trigger this.
       - Stores consent_session_id for upload validation
       - Returns: { phase, requestConsent, startRecording, stopRecording, consentSessionId, uri, metering, duration }

    2. Create mobile/src/features/recording/components/LiveConsentFlow.tsx:
       - Uses useLiveRecording hook
       - UI states:
         a. Requesting consent: "파트너에게 녹음 동의를 요청하고 있어요..." with loading spinner
            Show partner name if available, timer counting down from 5 minutes
         b. Waiting: Same as requesting, with pulsing animation
         c. Consent granted: "파트너가 동의했습니다!" brief success message, auto-proceed to recording
         d. Consent declined: "파트너가 녹음을 거부했습니다"
            Offer alternative: "혼자 기록하기로 전환할까요?" button -> switches to narration mode
            "취소" button -> goes back to mode selection
         e. Recording: Shows full RecordingControls + WaveformVisualizer (reuse from Plan 02)
            Persistent red bar at top: "녹음 중 - 파트너와 함께" with red dot
            Partner notification: "파트너도 녹음을 중지할 수 있습니다"
         f. Stopped: transition to RecordingPreview (reuse from Plan 02)
       - On submit: uploadAudio with type='live' and consentSessionId
       - After upload: poll status, navigate to transcript/[id]

    NOTE on partner-side stop: For v1, either partner can stop by pressing stop on their own device.
    The WebSocket channel can broadcast a 'recording_stopped' message. The partner's app receives
    this and also stops their local state. However, only ONE device is actually recording the audio
    (the initiator's device). The partner just sees a "recording in progress" screen with a stop button
    that sends a WebSocket message to the initiator's device to stop.
  </action>
  <verify>
    - `cd mobile && npx tsc --noEmit` passes
    - LiveConsentFlow renders all UI states correctly
    - useLiveRecording manages consent -> record -> upload lifecycle
    - Consent timeout is 5 minutes
    - Declined consent offers narration alternative
  </verify>
  <done>
    Live consent flow sends WebSocket consent request to partner. Handles acceptance, decline, and timeout. On acceptance, transitions to recording mode. Partner can remotely stop recording. Declined consent offers self-narration alternative.
  </done>
</task>

<task type="auto">
  <name>Task 2: Integrate live mode into RecordingScreen + backend consent validation</name>
  <files>
    mobile/src/features/recording/components/RecordingScreen.tsx
    backend/apps/consents/consumers.py
    backend/apps/audio/views.py
  </files>
  <action>
    1. Update RecordingScreen.tsx:
       - When user selects "함께 녹음하기" (live mode):
         a. Check if partner is connected (use usePartner hook)
         b. If no partner: show alert "파트너와 연결이 필요합니다" with link to partner connect
         c. If partner connected: render LiveConsentFlow component
       - The LiveConsentFlow handles the entire consent -> record -> upload -> navigate flow
       - Add "함께 녹음하기" button styling: red-ish background with mic icon, prominent
       - When LiveConsentFlow offers narration fallback after decline, switch mode back to narration

    2. Update backend/apps/consents/consumers.py:
       - Ensure the existing consent WebSocket consumer supports the recording consent flow:
         a. Handle 'consent_request' type: creates RecordingConsent record, sends to partner's channel
         b. Handle 'consent_response' type: updates RecordingConsent, sends result back to requester
         c. Handle 'stop_recording' type: broadcasts stop signal to the partner
       - If these message types aren't already handled (Phase 1 may have only set up the basic structure),
         add them. The RecordingConsent model already exists from Phase 1 with PENDING/BOTH_CONSENTED/DECLINED/EXPIRED states.

    3. Update backend/apps/audio/views.py upload_audio():
       - For live recordings, validate consent more thoroughly:
         a. Check RecordingConsent exists with matching session_id
         b. Check status is BOTH_CONSENTED
         c. Check consent hasn't expired (expires_at > now)
         d. Check the requester matches the uploading user (or their partner)
         e. Return 403 with Korean error if validation fails
       - Ensure the couple field is set on AudioRecording from the consent's couple
  </action>
  <verify>
    - `cd mobile && npx tsc --noEmit` passes
    - RecordingScreen shows both narration and live mode options
    - Live mode disabled when no partner connected
    - Backend consent validation rejects uploads without valid consent
    - `uv run python manage.py check` passes
  </verify>
  <done>
    RecordingScreen fully supports both self-narration and live conflict recording. Live mode requires partner consent via WebSocket. Backend validates consent before accepting live recording uploads. Partner can stop recording remotely. Declined consent gracefully falls back to narration mode.
  </done>
</task>

</tasks>

<verification>
- Live recording mode available when partner is connected
- Consent request sent via WebSocket and partner receives it
- Consent acceptance triggers recording, decline offers narration alternative
- Either partner can stop recording via WebSocket message
- Upload endpoint validates consent for live recordings
- Full flow: consent -> record -> upload -> transcribe with diarization -> transcript view
</verification>

<success_criteria>
- User can initiate live recording and partner receives consent request
- Partner acceptance enables recording to start
- Partner decline shows narration alternative
- Recording shows persistent red indicator for both partners
- Either partner can stop recording
- Upload validates consent session before processing
- Diarized transcript shows speaker-separated lines
</success_criteria>

<output>
After completion, create `.planning/phases/03-audio-pipeline/03-05-SUMMARY.md`
</output>
