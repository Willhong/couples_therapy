---
phase: 03-audio-pipeline
plan: 09
type: execute
wave: 1
depends_on: []
files_modified:
  - mobile/src/features/recording/components/LiveConsentFlow.tsx
  - mobile/src/features/recording/components/GuidedPrompts.tsx
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Cancel button in LiveConsentFlow has normal size and styling"
    - "Guided prompts show visual hint indicating they are tappable"
  artifacts:
    - path: "mobile/src/features/recording/components/LiveConsentFlow.tsx"
      provides: "Consent flow with fixed button styling"
      contains: "secondaryButton"
    - path: "mobile/src/features/recording/components/GuidedPrompts.tsx"
      provides: "Prompts with tap hint"
      contains: "tapHint"
  key_links: []
---

<objective>
Fix UI polish issues from UAT feedback.

Purpose: GAP-2 (prompt UX) and GAP-5 (button styling) are low-priority polish items
Output: Better visual affordances and consistent button styling
</objective>

<execution_context>
@C:\Users\Hong\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Hong\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@mobile/src/features/recording/components/LiveConsentFlow.tsx
@mobile/src/features/recording/components/GuidedPrompts.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix LiveConsentFlow cancel button styling</name>
  <files>mobile/src/features/recording/components/LiveConsentFlow.tsx</files>
  <action>
Review and fix the cancel button styling in LiveConsentFlow:

1. Check if secondaryButton style has proper sizing (the UAT noted "취소 버튼 크기 이상함")
2. Ensure consistent button styling across all phases:
   - All cancel/secondary buttons should use the same style
   - Verify paddingVertical is consistent (should be 14)
   - Verify borderRadius is consistent (should be 10)
   - Check that the button text is properly centered
3. Verify the buttonGroup has proper spacing (gap: 10 or 12)
4. If buttons look different sizes, it may be due to flex or width issues - ensure both buttons have same base dimensions
5. Add minHeight: 48 to both primaryButton and secondaryButton for touch target consistency
  </action>
  <verify>
- `npx tsc --noEmit` passes
- Cancel button appears same size as primary button in LiveConsentFlow screens
  </verify>
  <done>Cancel button has consistent, properly sized styling</done>
</task>

<task type="auto">
  <name>Task 2: Add visual hint to GuidedPrompts indicating tappability</name>
  <files>mobile/src/features/recording/components/GuidedPrompts.tsx</files>
  <action>
Improve GuidedPrompts UX by adding visual affordance that prompts are tappable:

1. Add a tap hint icon to the label row:
   ```tsx
   <View style={styles.labelRow}>
     <Text style={styles.label}>참고할 질문을 선택하세요</Text>
     <View style={styles.tapHint}>
       <Ionicons name="hand-left-outline" size={14} color="#9CA3AF" />
       <Text style={styles.tapHintText}>탭하여 선택</Text>
     </View>
   </View>
   ```
2. Import Ionicons from '@expo/vector-icons'
3. Add styles:
   ```tsx
   labelRow: {
     flexDirection: 'row',
     justifyContent: 'space-between',
     alignItems: 'center',
     paddingHorizontal: 16,
     marginBottom: 8,
   },
   tapHint: {
     flexDirection: 'row',
     alignItems: 'center',
     gap: 4,
   },
   tapHintText: {
     fontSize: 12,
     color: '#9CA3AF',
   },
   ```
4. Remove marginBottom from label style (moved to labelRow)
5. Optionally add subtle shadow or border to chips to make them look more interactive:
   ```tsx
   chip: {
     ...existing styles,
     shadowColor: '#000',
     shadowOffset: { width: 0, height: 1 },
     shadowOpacity: 0.05,
     shadowRadius: 2,
     elevation: 1,
   },
   ```
  </action>
  <verify>
- `npx tsc --noEmit` passes
- GuidedPrompts shows "탭하여 선택" hint next to label
- Prompts look more interactive/tappable
  </verify>
  <done>Guided prompts have clear visual indication they are tappable</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` - TypeScript compiles without errors
2. LiveConsentFlow cancel button has proper, consistent sizing
3. GuidedPrompts shows tap hint and prompts look interactive
</verification>

<success_criteria>
- Cancel button in consent flow has normal, consistent size
- Guided prompts clearly indicate they are tappable
- No TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-audio-pipeline/03-09-SUMMARY.md`
</output>
