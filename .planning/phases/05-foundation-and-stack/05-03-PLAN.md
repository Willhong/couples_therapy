---
phase: 05-foundation-and-stack
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/requirements.txt
autonomous: true

must_haves:
  truths:
    - "Backend dependencies are pinned to target versions (langgraph>=1.0.8, langchain-core>=1.2.11, langchain>=1.2.7)"
    - "Mobile dependencies are verified compatible (Expo 54 + RN 0.81 + React 19.1)"
    - "Existing tests still pass after dependency upgrades"
  artifacts:
    - path: "backend/requirements.txt"
      provides: "Pinned backend dependency versions"
      contains: "langgraph>=1.0.8"
  key_links:
    - from: "backend/requirements.txt"
      to: "backend/apps/intelligence/services/analysis_graph.py"
      via: "langgraph import"
      pattern: "from langgraph"
---

<objective>
Upgrade backend dependencies to target versions and verify mobile dependency compatibility.

Purpose: Ensure the stack is current (STAK-01) and mobile dependencies are compatible (STAK-02). Minor patch upgrades reduce risk of encountering already-fixed bugs in LangGraph/LangChain.
Output: Updated requirements.txt with pinned versions, verified mobile compatibility
</objective>

<execution_context>
@C:/Users/Hong/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Hong/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-foundation-and-stack/05-RESEARCH.md

@backend/requirements.txt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Upgrade backend dependencies to target versions (STAK-01)</name>
  <files>
    backend/requirements.txt
  </files>
  <action>
    Update `backend/requirements.txt` version pins for the LangChain ecosystem and psycopg. Change these lines:

    ```
    # BEFORE:
    langchain>=0.3.0
    langchain-core>=0.3.0
    langgraph>=0.2.0
    langchain-openai>=0.2.0
    langchain-anthropic>=0.3.0
    langchain-google-genai>=2.0.0
    psycopg[binary]>=3.1

    # AFTER:
    langchain>=1.2.7
    langchain-core>=1.2.11
    langgraph>=1.0.8
    langchain-openai>=1.1.7
    langchain-anthropic>=1.3.1
    langchain-google-genai>=4.2.0
    psycopg[binary]>=3.3.2
    ```

    These are the versions already installed (confirmed by research) -- we are pinning the floor to current versions so fresh installs get compatible versions.

    After updating requirements.txt, run the install to verify resolution:
    ```bash
    cd backend && uv pip install -r requirements.txt
    ```

    Then run existing tests to confirm nothing breaks:
    ```bash
    cd backend && uv run python manage.py test --settings=config.settings.development --verbosity=2
    ```

    If any test fails, check if the failure is pre-existing (not caused by dependency upgrade) by checking git status. Pre-existing failures should be noted but not fixed in this plan.
  </action>
  <verify>
    `grep "langgraph>=1.0.8" backend/requirements.txt`
    `grep "langchain-core>=1.2.11" backend/requirements.txt`
    `grep "langchain>=1.2.7" backend/requirements.txt`

    `cd backend && uv pip list | grep -i langgraph`
    Should show langgraph >= 1.0.8

    `cd backend && uv run python manage.py test --settings=config.settings.development`
    Should pass (or failures are documented as pre-existing).
  </verify>
  <done>
    requirements.txt pins LangChain ecosystem to >=1.2.x and LangGraph to >=1.0.8. Fresh install resolves successfully. Existing tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify mobile dependency compatibility (STAK-02)</name>
  <files></files>
  <action>
    This is a verification-only task. The mobile stack (Expo SDK 54, React Native 0.81.5, React 19.1.0) is already at current versions per research.

    Run Expo's compatibility checker:
    ```bash
    cd mobile && npx expo-doctor
    ```

    Also verify the package.json has current versions:
    ```bash
    cd mobile && node -e "const p = require('./package.json'); console.log('expo:', p.dependencies.expo); console.log('react-native:', p.dependencies['react-native']); console.log('react:', p.dependencies.react);"
    ```

    If `expo-doctor` reports any issues, document them. If no issues, STAK-02 is verified as compatible.

    Do NOT upgrade any mobile packages unless expo-doctor explicitly flags a critical incompatibility. The goal is verification, not modification.
  </action>
  <verify>
    `cd mobile && npx expo-doctor` exits without critical errors.

    Expo SDK 54, React Native 0.81.x, and React 19.x are confirmed in package.json.
  </verify>
  <done>
    Mobile dependencies verified compatible. expo-doctor reports no critical issues. Expo SDK 54, React Native 0.81.5, React 19.1.0 are current and compatible.
  </done>
</task>

</tasks>

<verification>
1. `backend/requirements.txt` has updated version pins
2. `uv pip install -r requirements.txt` succeeds
3. Existing backend tests pass
4. `npx expo-doctor` reports no critical issues
</verification>

<success_criteria>
- requirements.txt has langgraph>=1.0.8, langchain-core>=1.2.11, langchain>=1.2.7
- Backend install resolves without conflicts
- Backend tests pass (or pre-existing failures documented)
- Mobile expo-doctor clean
</success_criteria>

<output>
After completion, create `.planning/phases/05-foundation-and-stack/05-03-SUMMARY.md`
</output>
